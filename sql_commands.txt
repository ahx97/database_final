use this when inserting into users

INSERT INTO users (Name, PlateNumber, LotName) values (varchar, varchar, varchar);

use this when inserting into parkinglot

INSERT INTO parkinglot (AvailableSpots, Price, SellerID) values(int, float, int);
